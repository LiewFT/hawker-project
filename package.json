{
  "name": "mustgotoeat",
  "version": "0.1.0",
  "private": true,
  "description": "Singapore hawker/food court/cafe venue and stall data, validated in CI. Frontend is currently the plain static Makan Trail demo site; the /data model here is the Week 1 foundation for the real MustGoToEat build.",
  "scripts": {
    "validate-data": "node scripts/validate-data.js",
    "migrate-venues": "node scripts/migrate-venues.js",
    "sync-sheet": "node scripts/sync-sheet-to-json.js"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
